import 'package:flutter/material.dart';

abstract class PokemonColors {
  static final cardShadowColor = Colors.black.withValues(alpha: 0.2);
  static final cardOverlayColor = Colors.white.withValues(alpha: 0.1);
  static final cardPokeballColor = Colors.white.withValues(alpha: 0.14);
  static final cardPokemonNumberColor = Colors.black.withValues(alpha: 0.1);
  static const cardTextColor = Colors.white;
}
